version: 2

models:
  - name: core__fact_messages
    description: |-
      deprecating soon. This view has been renamed core__ez_messages to avoid confusion with fact_msgs and fact_msg_attributes.
    
    columns:
      - name: MESSAGE_ID
        description: "{{ doc('message_id') }}"

      - name: BLOCK_ID
        description: "{{ doc('block_id')}}"

      - name: BLOCK_TIMESTAMP
        description: "{{ doc('block_timestamp')}}"

      - name: TX_ID
        description: "{{ doc('tx_id') }}"

      - name: tx_succeeded
        description: "{{ doc('tx_succeeded') }}"

      - name: CHAIN_ID
        description: "{{ doc('chain_id') }}"

      - name: MESSAGE_INDEX
        description: "{{ doc('message_index') }}"

      - name: MESSAGE_TYPE
        description: "{{ doc('message_type') }}"

      - name: MESSAGE_VALUE
        description: "{{ doc('message_value') }}"

      - name: ATTRIBUTES
        description: "{{ doc('attributes') }}"
